# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# Google AI Studio API Key for generating book summaries
# SECURITY: Never commit real API keys to version control
# SECURITY: Use at least 20 characters for production keys
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_AI_API_KEY=your_api_key_here

# Flask Secret Key for session management
# SECURITY: Must be changed in production!
# SECURITY: Use a strong random value (32+ characters recommended)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# ============================================================================
# ENVIRONMENT CONFIGURATION
# ============================================================================

# Application environment (development, production)
# Set to 'production' for production deployments
ENVIRONMENT=development

# Database path (optional, defaults to bookshelf.db)
# DATABASE_PATH=bookshelf.db

# ============================================================================
# ADVANCED SECRET MANAGEMENT (Optional)
# ============================================================================

# Secret Provider Configuration
# Use 'aws' for AWS Secrets Manager or 'gcp' for Google Secret Manager
# CLOUD_SECRET_PROVIDER=aws

# For GCP Secret Manager (required if using 'gcp' provider)
# GCP_PROJECT_ID=your-project-id

# Directory for file-based secrets (Docker/Kubernetes)
# Defaults to /run/secrets (Docker Swarm/Kubernetes standard)
# SECRETS_DIR=/run/secrets

# ============================================================================
# PRODUCTION SECURITY SETTINGS (Optional)
# ============================================================================

# Force HTTPS redirects in production
# FORCE_HTTPS=true

# Port for web server (defaults to 5000)
# PORT=5000
